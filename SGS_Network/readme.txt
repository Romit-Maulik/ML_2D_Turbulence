Training data is given in numpy binary format - for compression
Function load_data() in the Keras_Training.py file loads the filenames and trains a DNN

The imported numpy array in Training_stencil is a matrix of 20 columns, corresponding to 20 inputs. The rows are samples
The imported numpy array in Training_source is a matrix of 1 column, corresponding to one output. The rows are samples.
